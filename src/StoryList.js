import React, { Component } from 'react'
import PropTypes from 'prop-types'

class StoryList extends Component {
    render() {
        return (
            //buttons w/ values for storing info and data will be added later
        )
    }
}

export default StoryList